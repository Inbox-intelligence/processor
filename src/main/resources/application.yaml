server:
  port: 8081

spring:
  application:
    name: processor

  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/inbox-intelligence
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  rabbitmq:
    host: ${RABBITMQ_HOST}
    port: ${RABBITMQ_PORT}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}
    listener:
      simple:
        default-requeue-rejected: false
        retry:
          enabled: true
          max-attempts: 3
          initial-interval: 1000
          multiplier: 2.0
          max-interval: 10000

rabbitmq:
  exchange: email-events
  queue: email-processed
  routing-key: email.processed

email-storage:
  provider: ${EMAIL_STORAGE_PROVIDER}

sanitization:
  quoted-text-patterns:
    - "(?m)^On .{10,80}<[^>]+>\\s*wrote:\\s*$"
    - "(?m)^On .{10,80} wrote:\\s*$"
    - "(?m)^-{3,}\\s*Original Message\\s*-{3,}"
    - "(?ms)^From:\\s.*\\nSent:\\s.*\\nTo:\\s.*"
    - "(?m)^_{3,}\\s*$"
  signature-patterns:
    - "^-- ?$"
    - "(?i)^Sent from my (iPhone|iPad|Galaxy|Samsung)"
    - "(?i)^Sent from Mail for Windows"
    - "(?i)^Get Outlook for (iOS|Android)"
    - "(?i)^Sent from Yahoo Mail"
  sign-off-phrases:
    - "best regards"
    - "kind regards"
    - "warm regards"
    - "regards"
    - "thanks"
    - "thank you"
    - "cheers"
    - "sincerely"
    - "respectfully"
    - "best"
  disclaimer-keywords:
    - "confidential"
    - "privileged"
    - "intended recipient"
    - "unauthorized"
    - "disclaimer"
    - "this email"
    - "this message"
    - "please notify"
    - "delete this"
    - "legally privileged"

logging:
  level:
    com.inboxintelligence.processor: DEBUG
    org.springframework.amqp: INFO
